# 稳定版 v2026.06.18-stable

本版本为**已验证可上线**基线，包含支付、PDF、院校检索、推广海报等修复。

## 版本信息

| 项目 | 值 |
|------|-----|
| 标签 | `v2026.06.18-stable` |
| 分支 | `release/stable-20260618` |
| 提交 | `ab56ef3`（及此前 fix-payment / fix-pdf-search 全部提交） |

## 已验证能力

- 虚拟支付现网（env=0，iPhone）
- 道具 ID：`xdptk`（普通卡 19.9 元）、`xdbjk`（白金卡 298 元）
- PDF 导出、院校检索、河南 48 志愿、推广海报
- 支付创建 500 / 502 恢复脚本

## 服务器配置快照（回退后需确认）

`ecosystem.config.js` 中建议保留：

```javascript
WECHAT_VIRTUAL_PAY_ENV: '0',
WECHAT_VIRTUAL_PRODUCT_TRIAL: 'xdptk',
WECHAT_VIRTUAL_PRODUCT_PREMIUM: 'xdbjk',
WECHAT_VIRTUAL_GOODS_PRICE_PREMIUM: '29800',
```

`ecosystem.secrets.js`：现网 AppKey、WECHAT_SECRET（勿提交 Git）。

验证命令：

```powershell
curl.exe -s https://api.zntb.lhyun.net/api/payments/wechat/status
# 应含 products.trial.product_id=xdptk, products.premium.product_id=xdbjk
```

## 回退方式

### 方式 A：一键脚本（服务器）

```powershell
powershell -ExecutionPolicy Bypass -File C:\zhiyuantianbao\scripts\rollback-to-stable.ps1
```

### 方式 B：Git 检出标签

```powershell
cd C:\zhiyuantianbao
git fetch origin tag v2026.06.18-stable
git checkout v2026.06.18-stable
pm2 delete zhiyuan-backend
pm2 start ecosystem.config.js
pm2 save
```

### 方式 C：检出稳定分支

```powershell
cd C:\zhiyuantianbao
git fetch origin release/stable-20260618
git checkout release/stable-20260618
pm2 delete zhiyuan-backend
pm2 start ecosystem.config.js
pm2 save
```

## 新功能开发建议

1. 从本标签或 `release/stable-20260618` 拉新分支，例如 `feature/xxx-0c75`
2. 开发完成前不要覆盖 `release/stable-20260618`
3. 出问题随时 `git checkout v2026.06.18-stable` 或跑回退脚本
